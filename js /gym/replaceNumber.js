// Write a function `replaceNumbers` that accepts a string containing both letters and numbers. The function should replace all numbers with an exclamation mark (`!`) and return the modified string.

// **Examples:**

// - `replaceNumbers("Hello123")` should return `"Hello!!!"`.
// - `replaceNumbers("2024 is here!")` should return `"!!!! is here!"`

// **Test cases:**

// - `replaceNumbers("No numbers")` should return `"No numbers"`.
// - `replaceNumbers("3 blind mice")` should return `"! blind mice"`.